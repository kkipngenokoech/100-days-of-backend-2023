import 'package:flutter/material.dart';

class Multiverse extends StatelessWidget {
  const Multiverse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}