import 'package:flutter/material.dart';

class CartoonNetwork extends StatelessWidget {
  const CartoonNetwork({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}